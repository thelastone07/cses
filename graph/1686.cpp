#include<bits/stdc++.h>
using namespace std;

void solve() {
    int n, m;
    vector<int> coins(n+1);
    for (int i = 0; i < n; i++) cin >> coins[i+1];
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}